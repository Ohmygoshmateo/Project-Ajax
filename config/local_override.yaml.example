# Copy to config/local_override.yaml (gitignored) and set only what you want to
# change. Values here are merged over config/default.yaml.
#
# account:
#   equity: 25000.0
#   risk_pct_per_trade: 0.05
#
# options:
#   delta_min: 0.45
#   delta_max: 0.60
